# ML 신호 품질 필터 — 학습 환경 전용 (로컬 PC).
# AWS 운영 서버에는 설치하지 않음. AWS는 requirements.txt(추론 최소셋)만 유지.
#
# 설치:
#   python -m venv venv-ml
#   source venv-ml/bin/activate    # Windows: venv-ml\Scripts\activate
#   pip install -r requirements-ml.txt

# ── 핵심 ML ────────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.4.0
joblib==1.3.2

# ── Feature ────────────────────────────────────────
pandas-ta==0.3.14b
pyarrow==15.0.0          # Parquet
pandera==0.18.0          # 스키마 검증 (lookahead 방지)

# ── 튜닝 / 설명력 ──────────────────────────────────
optuna==3.5.0
shap==0.44.1
matplotlib==3.8.2

# ── MLOps (선택, 로컬 분석용) ──────────────────────
mlflow==2.10.0           # 로컬 SQLite 백엔드
evidently==0.4.16        # 드리프트 리포트

# ── 공통 의존 (이미 메인 requirements에 있을 가능성) ──
pandas>=2.0
numpy>=1.24
