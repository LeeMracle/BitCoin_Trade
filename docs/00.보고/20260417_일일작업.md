# 일일작업 — 2026-04-17

## 오늘 목표

| WBS ID | 태스크 | 목표 (DoD) | 결과 | 비고 |
| ------ | ------ | ---------- | ---- | ---- |
| P7-03  | heartbeat 파일 갱신 (realtime_monitor) | 매 5분 `/tmp/bata_heartbeat` touch 구현 + 로컬 검증 | ✅ 완료 | 로컬 구현 + AWS 배포, 04-11 목표일 6일 지연 회수 |
| P7-04  | watchdog_check.sh + cron 등록 | 10분 미갱신 시 텔레그램 경보 + 자동 재시작 | ✅ 완료 | cron `* * * * *` 등록, 서버 반영 |
| P7-05  | systemd WatchdogSec 설정 | WatchdogSec=300 + sd_notify 연동 | ✅ 완료 | Type=notify + WatchdogUSec=5min, TimeoutStartSec=600(초기화 4분 대응) |
| P7-06  | State ↔ Exchange 교차검증 (매 1시간) | state.positions vs get_balance 비교, 불일치 경보 | ✅ 완료 | realtime_monitor `_hourly_sync` 구현 |
| P7-07  | 웹소켓 stale connection 감지 | 5분 무수신 시 강제 재연결 + 경보 | ✅ 완료 | 5분 timeout + TimeoutError 처리 |
| P7-08  | 로그 볼륨 감시 cron | 일일 0줄/5000줄+ 이상 경보 | ✅ 완료 | cron `10 0 * * *` (09:10 KST) 등록 |
| P5-28b | VB 개선 재검증 (DRY-RUN 재집계) | 04-10~16 로그 7일치 누적 재평가 | ⏸ HOLD | 로컬 세션 SSH 제약으로 서버 집계 스크립트 템플릿만 작성, 04-18 서버 실행 필요 |
| P6-09  | R6: async ccxt fetch_* 누락 await 린트 | lint_none_format.py R6 추가 + 탐지 건수 기록 | ✅ 완료 | R6 5건 탐지 (WARN), lessons/20260417_1 등록 |
| P6-10  | R7: 상태 파일 진입 시 fetch_balance 교차검증 린트 | lint_none_format.py R7 추가 + lessons#10 연계 | ✅ 완료 | R7 규칙 추가 |
| P6-11  | R8: 시장가 주문 직후 sleep 없는 fetch_order 린트 | lint_none_format.py R8 추가 + 탐지 건수 기록 | ✅ 완료 | R8 규칙 추가 |

## 진행 현황

- [x] P7-03 heartbeat 파일 갱신 (realtime_monitor)
- [x] P7-04 watchdog_check.sh + cron 등록
- [x] P7-05 systemd WatchdogSec 설정
- [x] P7-06 State ↔ Exchange 교차검증 (매 1시간)
- [x] P7-07 웹소켓 stale connection 감지
- [x] P7-08 로그 볼륨 감시 cron
- [ ] P5-28b VB 개선 재검증 (DRY-RUN 재집계) — 서버 집계 대기
- [x] P6-09 R6 async await 누락 린트
- [x] P6-10 R7 fetch_balance 교차검증 린트
- [x] P6-11 R8 시장가 주문 직후 fetch_order 린트

## 작업 결과

| WBS ID | 태스크 | 결과 |
| ------ | ------ | ---- |
| P7-03~08 | 모니터링 3계층 | CTO gate PASS → 배포 완료. 서비스 active(running), Type=notify, WatchdogUSec=5min. 최초 배포 시 `TimeoutStartSec` 미설정으로 1회 실패 → 600초로 상향 후 성공. 신규 lesson 20260417_2 필요 |
| P6-09~11 | 린트 R6~R8 | `scripts/lint_none_format.py` 확장, pre_deploy_check 연계. ERROR 0 / WARN 108 (기존 누적 포함), R6 5건은 의도된 lesson 등록 상태 |
| P5-28b | VB 7일 재검증 | 재집계 스크립트 템플릿 완성(`workspace/reports/20260417_vb_drymake_7day_recheck.md`). 서버 접근 제약으로 04-18 AWS에서 실행 후 §7~§9 작성 필요 |
| 부수 | pdca-pm 위임 구조 검증 | 10개 태스크 중 9개 완료, pdca-pm(builder)→상위 세션(gate/deploy) 교차검증 체계 정상 작동 |

## 추가 실행 (오후 긴급)

사용자 지시: BTC 분할매도 추적 확인 → cron 재소실 발견 → 즉시 조치

| WBS ID | 태스크 | 결과 |
| ------ | ------ | ---- |
| P4-14c | jarvis cron 재발 방지 + deploy 스크립트 반영 | **완료** — `scripts/deploy_to_aws.sh`에 `CRON_JARVIS="0 * * * *"` 추가, `pre_deploy_check.check_jarvis_automation` WARN→ERROR 승급. 배포 시 서버 cron 자동 재등록 |
| P4-14d | btc-trader 재시작 루프 조사·해결 | **완료** — 원인: heartbeat 300s==WatchdogSec 경계 + `_refresh_levels()` 4분 블로킹 ping 공백. 조치: heartbeat 120s + refresh 진행 루프 내 `_sd_watchdog()` 호출. 배포 후 SIGABRT 재발 없음 |
| (신규) | lesson `20260417_2_systemd_notify_timeout_start.md` | **완료** — TimeoutStartSec 기본 90초 함정 + watchdog ping 주기 2가지 후속 교훈 기록. `pre_deploy_check.check_service_watchdog_sec`에 `TimeoutStartSec≥300` 검증 추가 |
| (부수) | CTO gate PASS → deploy | **완료** — 06:20:44 UTC `active(running)`, 4분 activating 안쪽 복귀, 2/5 포지션 유지, 메모리 236MB 여유 |

### 관찰된 후속 과제 (티켓 신규)
- 잔고 조회 스팸 로그 (`SOLO/KRW 마켓 없음` 등) 초당 수회 반복 — `log_volume_check.sh` 임계(5000) 쉽게 초과 가능. throttle 적용 후속 PR 필요.

## BTC 3단계 분할매도 진행 상태 (참조)

- **tp1 완료** (04-08 13:05, 0.00055193 BTC @ 105.8M) / 잔량 0.00128784 BTC
- **tp2**: price > dc20_high(112.00M) — 현재 110.28M, +1.55% 필요 (대기)
- **tp3**: fg > 50 — 현재 21 (Extreme Fear, 대기)
- **sl**: price < dc20_low(99.21M) — 미충족
- 자비스 실행기는 이제 매시 정각 자동 평가 (P4-14c 조치 완료)

## 이월 항목

| WBS ID | 태스크 | 사유 | 이월일 |
| ------ | ------ | ---- | ------ |
| P5-28b | VB 개선 재검증 (DRY-RUN 재집계) | 로컬 SSH 제약으로 서버 vb_state.json/journalctl 접근 불가, 집계 스크립트는 준비됨 | 2026-04-18 |
