# 일일작업 — 2026-04-09

## 오늘 목표

| WBS ID | 태스크 | 목표 (DoD) |
|--------|--------|------|
| **P4-18** | **CB L2 구현 (ADR 후속)** | circuit_breaker.py에 L2(-25% 강제 청산) + L1 95% 자동해제 + 단위 테스트 16건 PASS + pre_deploy_check 검증규칙 1건 추가 |
| **P4-19** | **CB L2 배포 검증** | lint ERROR 0 + 단위 테스트 PASS + pre_deploy_check PASS + git 커밋/푸시 (AWS 배포는 다음 세션) |
| P4-05 | VB DRY-RUN 검증 마감 *(이월)* | 7일차 로그 집계 — 우선순위 P4-18 뒤 |
| P4-06 | VB 실전 전환 판단 *(이월)* | P4-05 연쇄 |
| - | EMA50+200 DRY-RUN 신호 점검 *(이월)* | 로그 스냅샷 |
| - | BCH 수동 청산 경위 조사 *(이월)* | R5 잔여 |
| - | L8 upbit_client 알트 합산 재점검 *(이월)* | pre_deploy_check 경고 잔존 |

### DoD 상세 (P4-18)

1. `services/execution/circuit_breaker.py`
   - `CIRCUIT_BREAKER_L2_THRESHOLD`, `L1_AUTO_RESUME_PCT` config 참조
   - `check_and_trigger_l2(total_krw) -> bool` — 임계 돌파 시 상태 파일에 `l2_triggered_at` 기록
   - `is_l2_triggered() -> bool`
   - `check_l1_auto_resume(total_krw) -> bool` — 95% 회복 시 L1 자동 해제 (L2는 수동만)
2. `services/execution/config.py`
   - `CIRCUIT_BREAKER_L2_THRESHOLD = -0.25`
   - `CIRCUIT_BREAKER_L1_AUTO_RESUME_PCT = 0.95`
3. `services/execution/realtime_monitor.py`
   - 레벨 갱신 루프 초입에서 `check_and_trigger_l2` → 발동 시 `_liquidate_all_positions()` 호출 (시장가 전량)
   - `is_triggered()` 체크 경로에 `check_l1_auto_resume` 호출
   - 텔레그램 경보 + stdout 로그
4. `tests/execution/test_circuit_breaker_l2.py` — 최소 6케이스
5. `scripts/pre_deploy_check.py` — `check_cb_l2_config` 추가
6. 린트/테스트/pre_deploy_check 전부 GREEN
7. git 커밋 + push

## 진행 현황

- [x] 일일작업 파일 생성
- [x] circuit_breaker.py L2 로직 구현 (L1+L2+L1자동해제, 레거시 스키마 보강)
- [x] config.py L2 상수 추가 (L2_THRESHOLD=-0.25, L1_AUTO_RESUME_PCT=0.95)
- [x] realtime_monitor.py L2 호출 + `_liquidate_all_positions` + L1 자동해제 훅
- [x] _execute_buy L2 가드 이중화
- [x] tests/execution/test_circuit_breaker_l2.py 16건 작성 — **16/16 PASS**
- [x] pre_deploy_check `check_cb_l2_config` 추가 — GREEN
- [x] lint_none_format ERROR 0 (WARN 1건 BOM, 범위 외)
- [x] 전체 테스트 32/32 PASS
- [ ] WBS 갱신 (P4-18/P4-19 완료 처리)
- [ ] git 커밋 + push

## 작업 결과

| WBS ID | 태스크 | 결과 |
|--------|--------|------|
| **P4-18** | CB L2 구현 | circuit_breaker.py에 L2/L1자동해제 3함수 추가, 상태 스키마 확장 + 레거시 보강, config 상수 2개, realtime_monitor 주기 체크 훅 + `_liquidate_all_positions` + L2 가드 이중화 |
| **P4-18** | 단위 테스트 | tests/execution/test_circuit_breaker_l2.py 16건 전부 PASS (L1/L2 경계·재트리거·독립·자동해제·L2중 차단·disabled·스키마·레거시 호환) |
| **P4-18** | pre_deploy_check | `check_cb_l2_config` 추가 — L1/L2/resume 상수·심볼·realtime 연결 검증 |
| **P4-19** | 로컬 배포 검증 | lint ERROR 0, 전체 32/32 PASS, pre_deploy_check 0 error (AWS scp 배포는 다음 세션) |

## 이월 항목

| WBS ID | 태스크 | 사유 | 이월일 |
|--------|--------|------|--------|
| P4-05 | VB DRY-RUN 7일 검증 마감 | P4-18 우선 처리 | 2026-04-10 |
| P4-06 | VB 실전 전환 판단 | P4-05 연쇄 | 2026-04-10 이후 |
| - | P4-19 AWS 서버 scp 배포 | 로컬 검증까지 완료, 배포 액션 분리 | 2026-04-10 |
| - | EMA50+200 DRY-RUN 신호 점검 | 우선순위 | 2026-04-10 |
| - | BCH 수동 청산 경위 조사 | R5 잔여 | 2026-04-10 이후 |
| - | L8 upbit_client 알트 합산 재점검 | pre_deploy_check 경고 잔존 | 2026-04-10 |
| P6-07~11 | 린트 R4~R8 규칙 확장 | 계획대로 점증 | 04-15 ~ 04-17 |

## DoD 달성 결과

- [x] circuit_breaker.py L2/L1자동해제 구현
- [x] config 상수 추가
- [x] realtime_monitor 훅 연결
- [x] 단위 테스트 16건 ≥ 6건 (목표 초과)
- [x] pre_deploy_check 검증규칙 1건 추가
- [x] lint/test/pre_deploy GREEN
- [ ] git 커밋 + push (진행 중)
- [ ] AWS 배포 (이월, P4-19 일부)

**DoD 자체 평가**: P4-18 본체는 100% 달성. P4-19는 로컬 검증까지 완료했고 scp 배포만 이월 (네트워크/서버 작업은 별도 세션에서 안전하게 처리).
