# 일일작업 — 2026-04-18

> W16 토요일 / 팀 풀 스윕 — 지연 티켓 + 신규 티켓 일괄 해소 세션

## 오늘 목표

| WBS ID | 태스크 | 목표 (DoD) | 결과 | 비고 |
| ------ | ------ | ---------- | ---- | ---- |
| P5-28b | VB 개선 재검증 (DRY-RUN 재집계) | AWS `vb_state.json` + journalctl 7일치 집계 → 판정 | ✅ 완료(조건부) | 거래 0건, A필터 7일 연속 의도대로 차단 — **CONDITIONAL** |
| (신규) | 잔고 조회 스팸 로그 throttle | `SOLO/KRW 마켓 없음` 등 60초 throttle | ✅ 완료 | `services/common/log_throttle.py` 신규, 5/5 tests |
| P7-09 | 필터 작동 통계 카운터 | F&G/EMA200/ATR/CB/VB-A 집계 + JSON 영구화 | ✅ 완료 | `services/execution/filter_stats.py` 신규, realtime_monitor 훅 6곳, 9/9 tests |
| P7-10 | 일일 보고에 필터 통계 포함 | daily_report.py 섹션 추가 | ✅ 완료 | "🛡 필터 차단 통계" 섹션 |
| P6-12 | 메타 린트 (lessons ↔ 규칙 매핑) | lint_meta.py + 미연결 경고 | ✅ 완료 | `scripts/lint_meta.py`, 17 lessons 중 11 매핑 / 미집행 6건 경고, 12/12 tests |
| P7-11 | CTO 재검증 (모니터링 전체) | gate PASS → deploy → health 확인 | ✅ 완료 | 서비스 active, XRP 실측 차단(ema200_filter +1) |

## 진행 현황

- [x] P5-28b VB 개선 재검증 (조건부 완료)
- [x] 잔고 조회 스팸 로그 throttle 적용
- [x] P7-09 필터 작동 통계 카운터
- [x] P7-10 일일 보고 필터 통계 섹션
- [x] P6-12 메타 린트
- [x] P7-11 CTO 재검증 + 배포
- [x] WBS 주간 마일스톤 W16 갱신
- [x] 텔레그램 완료 보고

## 작업 결과

| WBS ID | 태스크 | 결과 |
| ------ | ------ | ---- |
| P5-28b | VB 7일 재검증 | 서버 집계 결과 04-10~04-17 VB 거래 **0건**. A필터(BTC<EMA200)가 7일 연속 정상 차단 — **의도된 동작**. 판정: **CONDITIONAL**(샘플 부족). VB 모듈 유지, 상승장 복귀 후 재집계 예정. `workspace/reports/20260417_vb_drymake_7day_recheck.md` §7~§10 완성 |
| 신규 throttle | 잔고 로그 스팸 | `throttled_print(key, msg, interval_sec=60)` 공용 헬퍼 신규. upbit_client.py의 2곳 적용. log_volume 임계(5000줄) 보호. 5/5 tests PASS |
| P7-09 | 필터 카운터 | `filter_stats.py`에 `record_block/snapshot/reset_today` 구현. realtime_monitor 6곳 훅(fg_gate/ema200_filter/atr_filter/cb_l1/cb_l2/vb_gate_a_bearish). `workspace/filter_stats.json` 자동 영구화(1분 flush). 자정 롤오버 시 history.jsonl append. 9/9 tests PASS |
| P7-10 | 일일보고 확장 | `scripts/daily_report.py`에 "🛡 필터 차단 통계 (24h)" 섹션 추가. 하락장/F&G/ATR/CB-L1/L2/VB-A 건수 렌더링 |
| P6-12 | 메타 린트 | `scripts/lint_meta.py` 신규 — lessons의 "## 검증규칙" 섹션을 파싱, `lint_none_format.py` R1~R8 및 `pre_deploy_check.py` check_* 함수와 매핑. **결과: 17 lessons 중 11 매핑, 미집행 6건 탐지**(기존 부채). `docs/lint_layer.md` Phase 5 섹션 추가. 12/12 tests PASS |
| pre_deploy_check 통합 | 신규 검증 4건 | `check_balance_log_throttle`, `check_filter_stats_integration`, `check_daily_report_filter_section`, `check_meta_lint_script` 추가. 전체 GREEN |
| P7-11 | CTO gate/deploy | gate PASS(8/8 항목) → AWS 서버 배포 완료(14개 파일 전송) → 서비스 active(Type=notify, WatchdogSec=300). **실측 검증**: 배포 직후 XRP/KRW 매수 시그널이 `ema200_filter`로 차단되어 `filter_stats.json`에 카운터 +1 기록 |

## 팀 가동 구조 (병렬 서브에이전트)

| 세션 | 담당 | 산출물 |
|------|------|-------|
| pdca-builder #1 | throttle | log_throttle.py + upbit_client 수정 + 5 tests |
| pdca-builder #2 | 필터 카운터 + 일일보고 | filter_stats.py + realtime_monitor 훅 + daily_report 섹션 + 9 tests |
| pdca-builder #3 | 메타 린트 | lint_meta.py + lint_layer.md Phase5 + 12 tests |
| 자비스 (메인) | VB 집계, plan, WBS, pre_deploy 통합, gate/deploy, 텔레그램 | workspace/plans/20260418, WBS, pre_deploy_check +4함수 |

3명의 builder는 파일 충돌 없이 병렬 실행. main이 pre_deploy_check 통합 + cto gate/deploy 수행.

## 교차검증 기록

- 검증 주체: B (cto gate 서브에이전트) + C (pre_deploy_check, lint_none_format, pytest, lint_meta)
- 확인 항목: 10개 (AC-1~10, §20260418_team_full_sweep.md)
- 발견 이슈: 0건 (신규 회귀 없음). lint_meta 미집행 6건은 기존 부채로 분리 관리
- 판정: **PASS**

## 서버 상태 (배포 후)

- `systemctl is-active btc-trader` → **active (running)**
- Type=notify, WatchdogSec=300, TimeoutStartSec=600 (lessons/20260417_2 준수)
- 메모리 487/911Mi used (424Mi available — 교훈 #5 통과)
- 포지션: RVN/KRW, TRX/KRW 2건 유지 (배포 전/후 동일)
- 필터 카운터 실측: `ema200_filter: {XRP/KRW: 1}` — 정상 동작

## BTC 3단계 분할매도 진행 상태 (참조)

- **tp1 완료** (04-08 13:05, 0.00055193 BTC @ 105.8M) / 잔량 0.00128784 BTC
- **tp2**: price > dc20_high(112.00M) — 대기
- **tp3**: fg > 50 — 대기 (F&G=21, Extreme Fear 유지)
- **sl**: price < dc20_low(99.21M) — 미충족
- 자비스 실행기는 매시 정각 자동 평가 (P4-14c)

## 다음 주 (W17, 04-20~04-26) 예고

- P6-13 (lint_history.jsonl 누적, 04-27 목표) — 스트레치
- VB 샘플 수집 모니터링 — BTC>EMA200 복귀 후 7일 시점에 재집계 자동 트리거
- P5-04 레짐 자동 전환 시스템 — 하락장 장기화로 우선순위 상향 예정
- lint_meta 미집행 6건 해소 티켓 발행 검토

## 이월 항목

없음 — 오늘 목표 전체 해소.

---

## 2차 세션 — Phase 5/6 전체 스윕 (사용자 "모두 진행" 지시)

### 2차 목표

| WBS | 태스크 | 결과 |
|-----|--------|------|
| P5-04 | 레짐 자동 전환 시스템 | ✅ 완료 (DRY-RUN) |
| - | lint_meta 미집행 6건 해소 | ✅ 완료 (17/17 매핑) |
| - | 명칭 정리 + VB 재집계 트리거 | ✅ 완료 |
| P6-13 | lint_history 누적 + 주간 통계 | ✅ 완료 |
| P5-02/03 | 리서치 문서 | ✅ 리서치 완료, 실행 대기 |

### 2차 작업 결과

| 영역 | 결과 |
|------|------|
| P5-04 | `services/execution/regime_switcher.py` + `scripts/regime_check.py` 신규. BULL(BTC>EMA200 AND F&G≥40) / BEAR(BTC<EMA200 OR F&G<20) / SIDEWAYS. 히스테리시스 3회. `workspace/regime_state.json` 영구화. REGIME_SWITCH_ENABLED=False(DRY-RUN). 26/26 tests |
| lint_meta 6건 | `pre_deploy_check.py`에 검증 22~27 추가 (tick_vs_bar / backtest_regime_split / post_fill_safety / cb_existing_positions / hard_stop_caps / startup_retry_backoff). lint_meta 17/17 매핑, 오류 0건 |
| 명칭 정리 | `scanner.py`에서 `fetch_btc_above_sma` 하위호환 alias 제거. 운영 코드의 `_btc_above_sma` 0건 (docs/lessons 이력만 잔존, 허용) |
| VB 자동 재집계 | `scripts/vb_recheck_trigger.py` 신규 — BTC 일봉 EMA200 7일 연속 돌파 + 7일 쿨다운 시 자동 리포트 생성 + 텔레그램. cron 09:15 KST 등록. 7/7 tests |
| P6-13 | `scripts/lint_history.py` 신규 — lint_none_format + lint_meta 결과를 `workspace/lint_history.jsonl`에 append. `--summary`(최근7) / `--weekly`(이번주). 10/10 tests |
| P5-02 리서치 | `docs/research/20260418_vb_param_optimization.md` — K 5값 × SL 4값 × vol 3값 = 60조합, in/out-sample 분할, 착수 트리거: vb_state.json history 15건+ |
| P5-03 리서치 | `docs/research/20260418_alt_pump_review.md` — 진입 3종/청산 4종 후보, 비용 왕복 0.20%, 착수 트리거: BULL 레짐 7일 유지 |
| 통합 (main) | `scripts/regime_check.py` 신규 — BTC 일봉 EMA200/SMA50 + F&G → decide_regime → state 갱신 → 전환 시 텔레그램. `pre_deploy_check.py`에 검증 28~30 추가 |

### 2차 팀 가동 구조 (병렬 4 builder)

| 세션 | 담당 | 산출물 |
|------|------|-------|
| pdca-builder #A | P5-04 레짐 전환 | regime_switcher.py + config 4상수 + 26 tests |
| pdca-builder #B | lint_meta 6건 | pre_deploy_check +6 함수, 17/17 매핑 달성 |
| pdca-builder #C | 명칭 + VB 트리거 | scanner.py 정리 + vb_recheck_trigger.py + cron + 7 tests |
| pdca-builder #D | P6-13 + P5-02/03 리서치 | lint_history.py + 리서치 문서 2건 + 10 tests |
| 자비스 (메인) | regime_check 통합 + pre_deploy_check 연장 + cto gate/deploy + 문서화 | regime_check.py + pre_deploy_check 검증 28~30 |

### 2차 교차검증

- 검증 주체: B (cto gate) + C (pre_deploy_check / lint_none_format / pytest / lint_meta / lint_history)
- 확인 항목: 18개 (AC-1~AC-18)
- 발견 이슈: 0건 (기존 경고 2건 — MAX_CONSECUTIVE_LOSSES 구현 미완은 별도 티켓)
- 전체 테스트: **126 passed**
- lint_meta: **17/17 매핑, 오류 0건**
- 판정: **PASS**

### 2차 배포 결과

- 전송 파일: 20개 (services 6 + scripts 11 + config 1 + common 2)
- 서비스: `active (running)`, Type=notify, WatchdogSec=300, TimeoutStartSec=600
- 필터 로그: `[v2] F&G=26 (허용) | BTC>EMA200=False` 정상
- 포지션: RVN/KRW, TRX/KRW 유지 (배포 전/후 동일)
- 메모리: 498/911Mi used (412Mi 여유)

### WBS 최종 진행현황 (04-18 종료 시점)

- Phase 0~4: 전부 완료
- Phase 5: 완료 28/30 (P5-02/03 리서치 완료·실행 대기)
- Phase 6: **완료 12/12 (전체 완료)**
- Phase 7: **완료 11/11 (전체 완료)**
- **합계: 완료 84/86 (97.7%), 대기 2건(P5-02/03 실행 대기)**

### 다음 단계 (W17 이후)

상승장 복귀 감지 시 자동 트리거될 항목:
- `regime_check` cron: BULL 레짐 3회 연속 감지 시 레짐 전환 알림
- `vb_recheck_trigger`: BTC EMA200 7일 돌파 시 VB 재집계 자동 실행
- P5-02/P5-03: regime BULL 7일 유지 시 리서치 문서 기준으로 즉시 착수 가능

하락장 중 지속 모니터링:
- `scripts/lint_history.py` 매주 일요일 --weekly로 주간 린트 추이 점검
- 신규 lesson 발생 시 lint_meta에 즉시 매핑 의무(이제 자동 탐지됨)
