"""자동매매 설정 파일.

이 파일만 수정하면 매매 동작이 변경됩니다.
"""

# ═══════════════════════════════════════════════════════
# 모니터링 주기 설정
# ═══════════════════════════════════════════════════════
# "realtime"  — 웹소켓 실시간 감시 (돌파 즉시 매수)
# "5m"        — 5분마다 스캔
# "10m"       — 10분마다 스캔
# "30m"       — 30분마다 스캔
# "1h"        — 1시간마다 스캔
# "4h"        — 4시간마다 스캔
# "daily"     — 하루 1회 (09:05 KST)

MONITOR_MODE = "realtime"

# ═══════════════════════════════════════════════════════
# 전략 설정
# ═══════════════════════════════════════════════════════
# 사용 가능한 전략:
#   "dc_atr"     — Donchian(50) + ATR(14)x3.0 (기존 메인 전략)
#   "rsi_ema"    — RSI(10) + EMA(150) (기존 보조 전략)
#   "ensemble"   — DC+ATR 과 RSI+EMA 앙상블 투표
#   "regime"     — 변동성 레짐별 ATR 배수 동적 조정
#   "mtf"        — EMA(200) 장기추세 + DC(30) 단기 진입
#   "volume"     — DC(50) + 거래량 확인 필터
#   "composite"  — DC + RSI + 거래량 + 적응형 스탑 (추천)
#   "daytrading" — 4시간봉 단타 (DC15+거래량돌파+트레일2%)

STRATEGY = "composite"

# 전략 파라미터 오버라이드 (get_strategy에 전달)
# 기본값은 각 전략의 make_strategy_XXX 기본값 사용
# 예: {"dc_period": 15} → composite 전략의 DC 기간을 15일로 축소 (더 공격적)
# 04-26 ADR 20260426-1: 20→15 (멀티코인 OOS Sharpe 0.624→0.803 검증)
STRATEGY_KWARGS = {"dc_period": 12}  # ADR 20260505-1: DC15→12 (신호 빈도 ~25% ↑, ATR 캡으로 손실 별도 차단)

DONCHIAN_PERIOD = 12          # Donchian 채널 기간 (일) — 50→20→15→10→15→12 (ADR 20260505-1: 매매 0건 30h 후 적극화, ATR 10% 캡 동시 적용)
ATR_PERIOD = 14               # ATR 기간 (일)
ATR_MULTIPLIER = 3.0          # ATR 트레일링스탑 배수

# ─── 하드 손절 캡 (lessons/20260408_5_ong_wide_stop.md) ───
# ATR*3 기반 스탑폭이 고변동 알트에서 28%+로 벌어지는 문제 방어.
# 진입 시/갱신 시 trail_stop이 entry * (1 - HARD_STOP_LOSS_PCT) 아래로
# 내려가지 않도록 상한을 강제한다. ONG -29.21% 사건(2026-04-06) 재발 방지.
HARD_STOP_LOSS_PCT = 0.10     # 단일 포지션 최대 손실률 (10%)

# 진입 전 변동성 필터 — ATR/price 비율이 이 임계값 이상이면 진입 차단
# (예: 0.08 = ATR이 가격의 8% 이상인 종목은 너무 변동성 큼)
MAX_ATR_PCT = 0.10            # 10% — ADR 20260505-1: 12%→10% lessons #13 안전선 복귀 (실측 차단 0건 → 효과 X, 미래 ONG 같은 고변동 위험 제거)

# ═══════════════════════════════════════════════════════
# 포지션 관리
# ═══════════════════════════════════════════════════════
MAX_POSITIONS = 10            # 최대 동시 보유 종목 수 (05-04 7→10 확대, 마지막 슬롯 자금 집중 완화)

# ═══════════════════════════════════════════════════════
# 부분 익절 (decision 20260504-1, plan 20260504_2 AC1)
# ═══════════════════════════════════════════════════════
# 진입 후 단계별 매도 — 큰 수익 일부 확보, 평균 수익 안정화
# 잔량 10%는 트레일 스탑(ATR×3 + 하드캡 -10%)으로 보유
TP_LEVELS = [
    {"trigger_pct": 0.05, "sell_ratio": 0.30},  # +5% → 30% 매도
    {"trigger_pct": 0.10, "sell_ratio": 0.30},  # +10% → 30% 매도
    {"trigger_pct": 0.15, "sell_ratio": 0.30},  # +15% → 30% 매도
]
TP_ENABLED = True

# ═══════════════════════════════════════════════════════
# 거래량 필터 (decision 20260504-1, plan 20260504_2 AC6-9)
# ═══════════════════════════════════════════════════════
# DC 돌파 + 거래량 5일 평균 × N배 이상 시만 매수 (가짜 돌파 차단)
VOL_FILTER_ENABLED = True
VOL_FILTER_MULTIPLIER = 1.0  # ADR 20260505-1: 1.2→1.0 (5-3~5-5 거래량 필터 4,102건 차단, 매매 빈도 ↑ 위해 평균만 요구)

# ═══════════════════════════════════════════════════════
# 일일 손실 한도 (decision 20260504-1, plan 20260504_2 AC10-14)
# ═══════════════════════════════════════════════════════
# 24h 누적 실현손익이 기준자본 × N% 손실 도달 시 신규 매수 차단
# 기존 포지션 트레일링 스탑은 정상 작동, 매일 09:00 KST 자동 reset
DAILY_LOSS_LIMIT_ENABLED = True
DAILY_LOSS_LIMIT_PCT = 0.03                      # 3%
DAILY_LOSS_BASE_KRW = 300_000   # 분모: 초기자본 (CIRCUIT_BREAKER_INITIAL_CAPITAL과 동일, cto M2)

POSITION_RATIO = 0.95         # 슬롯당 자금 사용 비율 (5% 여유)

# ═══════════════════════════════════════════════════════
# 종목 필터
# ═══════════════════════════════════════════════════════
MIN_VOLUME_KRW = 300_000_000     # 24h 최소 거래대금 (3억원) — plan 20260503_5: 5억→3억 (종목 풀 117→약 150)
MIN_LISTING_DAYS = 60             # 최소 상장일수

EXCLUDE_SYMBOLS = [               # 제외 종목 (스테이블코인 등)
    "USDT/KRW", "USDC/KRW", "DAI/KRW", "BUSD/KRW",
]

# ═══════════════════════════════════════════════════════
# 주문 설정
# ═══════════════════════════════════════════════════════
MIN_ORDER_KRW = 5_000         # 업비트 최소 주문 (전략 검증 단계 — 매매 빈도 우선)
DRY_RUN = False               # True: 주문 없이 신호만 확인

# ═══════════════════════════════════════════════════════
# 변동성 돌파 (VB) 병행 전략
# ═══════════════════════════════════════════════════════
# composite(스윙)과 병행하여 일일 단타 실행
# 매일 09:00 KST 레벨 갱신 시: 기존 VB 포지션 청산 → 새 신호 매수
# 실시간: VB 포지션 손절 감시

VB_ENABLED = True              # 변동성 돌파 병행 활성화
VB_DRY_RUN = True              # True: 가상매매만 (plan 20260503_2 — DRY-RUN 유지, LIVE 별도 검증)
VB_K_BULL = 0.4                # 상승장 K (공격적)
VB_K_NEUTRAL = 0.5             # 중립 K
VB_K_BEAR = 0.7                # 하락장 K (보수적)
VB_K_CRISIS = 0.85             # 극공포 K (F&G 20~30, 매우 보수적)
VB_SL_PCT = 0.020              # -2.0% 손절 (극공포 변동성 대응, 기존 1.5%→2.0%)
VB_SMA_PERIOD = 50             # 레짐 판별 SMA 기간
VB_MAX_POSITIONS = 3           # VB 전용 최대 슬롯
VB_POSITION_RATIO = 0.30       # VB에 할당할 자금 비율 (가용 현금의 30%)

# ─── VB 개선 (P5-28, docs/00.보고/20260409_일일작업.md) ───
# P4-06 NO-GO 판단(승률 36.8%, 하락장 악화 추세) 후속 — 04-15 재검증 목표
VB_BEAR_MARKET_FILTER = False      # plan 20260503_2: 거래 활성화 (BEAR에서도 VB 진입 허용, DRY-RUN 격리됨)
VB_DEAD_SYMBOL_THRESHOLD = 3       # B. N회 연속 0%/수수료손실 시 종목 영구 제외
VB_MAX_WEEKLY_PER_SYMBOL = 3       # C. 종목별 주간(ISO week) 최대 진입 횟수
VB_LOSS_COOLDOWN_N = 3             # D. N연속 손절 시 쿨다운 발동
VB_LOSS_COOLDOWN_HOURS = 24        # D. 쿨다운 기간 (시간)
VB_MIN_WINRATE = 0.35              # E. 재검증 시 최소 승률 기준 (기존 0.40 → 0.35)

# ═══════════════════════════════════════════════════════
# 레짐 필터 (v1.5 Sprint B — 200EMA)
# ═══════════════════════════════════════════════════════
# BTC/KRW 종가 < EMA(200)이면 전 종목 신규 매수 차단.
# 기존 포지션 트레일링스탑은 영향받지 않는다.
# False로 설정하면 EMA 필터 비활성 — 기존 SMA20 동작과 동일.
REGIME_FILTER_ENABLED = False  # plan 20260503_2: 거래 활성화 (BEAR 매수 차단 해제, 사용자 명시 승인)
REGIME_FILTER_EMA_PERIOD = 200

# ═══════════════════════════════════════════════════════
# EMA Trend Follow 전략 (DRY-RUN)
# ═══════════════════════════════════════════════════════
# BTC/KRW 일봉 EMA(50) 상향 돌파 + EMA(200) 필터 추세추종
# 매일 09:05 KST에 EMA 재계산, 실시간 체결가로 돌파 감지
# DRY_RUN=True 시 실제 주문 없이 텔레그램 "DRY-RUN 신호" 알림만 발송

EMA_TREND_ENABLED = True
EMA_TREND_DRY_RUN = True          # True: 가상매매만 (로그+알림, 실주문 없음)
EMA_TREND_EMA_PERIOD = 50         # 진입 EMA 기간 (돌파 감지용)
EMA_TREND_FILTER_PERIOD = 200     # 필터 EMA 기간 (하락장 진입 차단용)
EMA_TREND_TRAIL_PCT = 0.05        # 트레일링스탑 5% (고점 대비)
EMA_TREND_MAX_POSITIONS = 1       # BTC 단일 종목
EMA_TREND_POSITION_RATIO = 0.20   # 가용 현금의 20%

# ═══════════════════════════════════════════════════════
# 안전장치
# ═══════════════════════════════════════════════════════
MAX_CONSECUTIVE_ERRORS = 5    # 연속 오류 N회 시 봇 자동 중지
ERROR_COOLDOWN_SEC = 60       # 오류 후 동일 종목 재시도 대기 (초)
ALERT_COOLDOWN_SEC = 300      # 동일 오류 알림 간격 (초, 5분)

# ═══════════════════════════════════════════════════════
# 계좌 레벨 서킷브레이커
# ═══════════════════════════════════════════════════════
# 계좌 전체 평가금액이 초기자본 대비 THRESHOLD 이하로 떨어지면
# 모든 전략(Composite + VB)의 신규 매수를 자동 중단.
# 기존 포지션은 유지 (트레일링스탑은 계속 작동).
# 해제: 수동만 가능 (circuit_breaker_state.json 삭제 또는 triggered=false 설정)
CIRCUIT_BREAKER_ENABLED = True
CIRCUIT_BREAKER_THRESHOLD = -0.20          # L1: -20% 손실 시 발동 (신규 매수 차단)
CIRCUIT_BREAKER_INITIAL_CAPITAL = 300_000  # 초기 투자금 (KRW)

# L2 (2차 안전장치) — ADR 20260408_1
# L1 이후 추가 하락 시 기존 포지션까지 시장가 전량 청산.
# 해제: 수동만 가능 (catastrophic 시그널 → 사용자 원인 확인 필수).
CIRCUIT_BREAKER_L2_THRESHOLD = -0.25       # L2: -25% 손실 시 전량 청산
# L1 자동 해제: 총자산이 초기자본의 N% 이상 회복되면 L1 자동 해제.
# (L2는 자동 해제 없음)
CIRCUIT_BREAKER_L1_AUTO_RESUME_PCT = 0.95  # 95% 회복 시 L1 자동 해제

# ═══════════════════════════════════════════════════════
# 알림 설정
# ═══════════════════════════════════════════════════════
NOTIFY_ON_BUY = True          # 매수 시 텔레그램 알림
NOTIFY_ON_SELL = True         # 매도 시 텔레그램 알림
NOTIFY_DAILY_REPORT = True    # 일일 리포트
NOTIFY_NEAR_SIGNAL = True     # 근접 종목 (3% 이내) 알림

# ─── 레짐 자동 전환 (P5-04) ───
REGIME_SWITCH_ENABLED = False   # 초기 DRY-RUN: 판정 로직만 가동, 실거래 영향 없음
REGIME_HYSTERESIS_COUNT = 3     # 연속 몇 회 같은 신호에서 전환할지
REGIME_BULL_FG_MIN = 40
REGIME_BEAR_FG_MAX = 20
