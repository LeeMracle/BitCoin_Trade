# 블록 4 — 린트 R6/R7/R8 확장 (P6-09/10/11)

- **작성일(KST)**: 2026-04-17 09:35
- **작성자/세션**: pdca-pm (자비스)
- **예상 소요**: 1.5시간
- **관련 이슈/결정문서**: docs/lessons/20260408_4 (NoneType 포매팅 린트), docs/lessons/20260408_2 (state/balance), docs/lint_layer.md

## 1. 목표

`scripts/lint_none_format.py`에 R6/R7/R8 규칙을 추가하여 반복되는 실수 패턴을 AST 수준에서 자동 차단한다.

- **R6 (P6-09)**: async ccxt `fetch_*` 호출에 `await` 누락
- **R7 (P6-10)**: 상태 파일 로드 직후 `fetch_balance` 교차검증 누락 (lessons#10 연계)
- **R8 (P6-11)**: 시장가 주문 직후 `sleep` 없이 `fetch_order` 호출 (업비트 반영 지연)

## 2. 성공기준 (Acceptance Criteria)

- [ ] AC-1 (R6): async 컨텍스트에서 `exchange.fetch_balance/fetch_ticker/fetch_tickers/fetch_ohlcv/fetch_order` 호출 시 `await` 누락 탐지. 동기 ccxt(Exchange() 객체)에는 적용하지 않음(false positive 방지 heuristic: async def 내부 && attr 이름 매치만)
- [ ] AC-2 (R7): `load_state(...)` 또는 `json.load` 결과에서 `positions` 접근 후 동일 함수 내 `fetch_balance` 호출이 없으면 WARN. 스코프는 services/execution/*.py
- [ ] AC-3 (R8): `create_market_*_order` 또는 `create_order(..., type='market', ...)` 호출 직후 같은 함수 내 next 2개 문장 안에 `sleep(` 또는 `asyncio.sleep(` 없이 `fetch_order`가 나오면 WARN
- [ ] AC-4: 기존 R1~R5 회귀 없음 — 실행 시 ERROR 건수 이전과 동일
- [ ] AC-5: 새 규칙으로 탐지된 건수와 파일을 docs/lessons/20260417_*.md 또는 리포트에 기록
- [ ] AC-6: 교차검증 — qa가 규칙 정의서(lint_layer.md) 갱신 제안 + 실제 false positive 점검

## 3. 단계

1. 현재 lint 실행 결과 스냅샷(기준선)
2. R6/R7/R8 규칙 구현 (AST 기반)
3. 재실행 → 탐지 결과 정리
4. false positive 있으면 예외 규칙 추가
5. docs/lint_layer.md 또는 lessons 기록 갱신
6. pre_deploy_check가 린트를 포함하므로 자동 연동됨
7. 교차검증

## 4. 리스크

| 리스크 | 완화 |
|--------|------|
| R7의 "교차검증 누락" 정의는 주관적 | AST로 판별 가능한 명확한 패턴으로 좁힘: `state.positions` 이름 참조 + 같은 함수 내 `fetch_balance` 미호출 |
| R8의 market order 패턴이 다양 | upbit_client 문서화된 함수 이름 기반(`create_market_buy_order`, `create_market_sell_order`) 우선 |
| 기존 코드에서 대량 WARN 발생 | 우선 WARN(severity)로 등록, 필요시 억제 목록 추가. ERROR로 승급은 후속 PR |

## 5. 검증 주체

- [x] 옵션 B — pdca-qa (규칙 정의/false positive 검토)
- [x] 옵션 C — 자체 실행 후 ERROR 회귀 없음 확인

## 6. 회고

- (미완)
